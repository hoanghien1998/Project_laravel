type: object
description: Model for listing creating
required:
- car_model_id
- car_trim_id
- year
- price
properties:

  car_model_id:
    $ref: 'utils/Identifier.yaml'

  car_trim_id:
    $ref: 'utils/Identifier.yaml'

  year:
    $ref: 'utils/Year.yaml'

  price:
    $ref: 'utils/Money.yaml'
